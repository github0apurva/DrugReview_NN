
This location contains web API working on local system.

To run the API, please follow below steps:
  1) Copy the data and save it in local system
  2) Change the code (Web_API.py) to point towards this location
  3) Run the Web_API.py in command prompt (at the saved file location)
  4) Open Web Browser and type  a) localhost/5000
                                b) localhost/5000/model_train
                                b) localhost/5000/model_test
